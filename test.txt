import numpy as np

mask = self.df_companies["LIBELLE_GROUPE_nat"].astype(bool)
self.df_companies["LG"] = np.where(mask, self.df_companies["LIBELLE_GROUPE_nat"], self.df_companies["LIBELLE_GROUPE"])


