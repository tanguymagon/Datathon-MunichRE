        self.df_companies["CHIFFRE_AFFAIRES"] = self.df_companies.apply(
            lambda x: x["CHIFFRE_AFFAIRES_c"] if x["CHIFFRE_AFFAIRES_c"] > 0 else x["CHIFFRE_AFFAIRES"],
            axis=1,
        )
        self.df_companies["EXPOSITION"].fillna(0, inplace=True)
        self.df_companies["LG"] = self.df_companies.apply(
            lambda x: x["LIBELLE_GROUPE_nat"] if x["LIBELLE_GROUPE_nat"] else x["LIBELLE_GROUPE"],
            axis=1,
